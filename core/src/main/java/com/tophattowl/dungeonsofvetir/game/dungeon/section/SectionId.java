package com.tophattowl.dungeonsofvetir.game.dungeon.section;

/**
 * Identifies a distinct generation shape (algorithm family)
 * Each id is backed by a {@code LevelGenerator} registered in {@code DungeonGenerator}
 */
public enum SectionId {
    CAVES,
    RUINS

}
